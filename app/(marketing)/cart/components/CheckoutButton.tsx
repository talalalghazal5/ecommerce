import { Button } from '@/components/ui/button'
import React from 'react'

export default function CheckoutButton() {
    return (
        <Button className='w-full cursor-pointer'>Continue to checkout</Button>
    )
}
